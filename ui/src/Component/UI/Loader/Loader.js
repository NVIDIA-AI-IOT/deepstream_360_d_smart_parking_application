import React from 'react';

import classes from './Loader.css';

/**
 * Go to Loader.css to customize the loader for the browser.
 */
const loader = (props) => {
    return (
        <div className={classes.loader}>
            
        </div>
    );
};

export default loader;